[project]
name = "training-contract-based-testing"
version = "0.1.0"
description = ""
authors = [{ name = "Sander van Beek", email = "lakitna@gmail.com" }]
license = "MIT"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["schemathesis>=4.28.0,<5", "flask>=3.1.3,<4"]

# Virtual project: uv manages dependencies only, nothing is built or installed
# for this package itself (equivalent to Poetry's package-mode = false).
[tool.uv]
package = false
